# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  say-hello:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
  validation-job:
      working_directory: ~/project
      docker:
        - image: cimg/node:current
      steps:
        - checkout
        - restore_cache:
            key: npm-packages-{{ checksum "demo-app/package.json" }}-{{ checksum "demo-app/package-lock.json" }}
        - run:
            name: Do npm install and prerequisites preparation
            command: |
              cd demo-app
              npm install
        - run:
            name: Do static code analysis
            command: |
              cd demo-app
              npm run lint:ci
        - run:
            name: Run unit tests
            no_output_timeout: 5m
            command: |
              cd demo-app
              npm run test:ci
        - store_test_results:
            path: demo-app/reports
        - save_cache:
            paths:
              - "./demo-app/node_modules/"
              - "~/node_modules"
            key: npm-packages-{{ checksum "demo-app/package.json" }}-{{ checksum "demo-app/package-lock.json" }}
  build-job:
      working_directory: ~/project
      docker:
        - image: cimg/node:current
      steps:
        - checkout
        - restore_cache:
            keys:
              - npm-packages-{{ checksum "demo-app/package.json" }}-{{ checksum "demo-app/package-lock.json" }}
              - npm-packages-{{ checksum "demo-app/package.json" }}
              - npm-packages-
        - run:
            name: Do build of application
            command: |
              cd demo-app
              npm run build
        - persist_to_workspace:
            root: demo-app
            paths:
              - build
  deploy-job:
      working_directory: ~/project
      docker:
        - image: cimg/node:current
      steps:
        - checkout
        - restore_cache:
            keys:
              - npm-packages-{{ checksum "demo-app/package.json" }}-{{ checksum "demo-app/package-lock.json" }}
              - npm-packages-{{ checksum "demo-app/package.json" }}
              - npm-packages-
        - attach_workspace:
            at: demo-app
        - add_ssh_keys:
            fingerprints:
              - "d8:a6:92:64:0b:e9:e0:ac:83:58:46:0a:a0:c7:d2:19"
        - run:
            name: Do deploy of application
            command: |
              cd demo-app
              git config --global user.name "senad"
              git config --global user.email "senad.zaimovic@softhouse.se"
              npm run deploy

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  say-hello-workflow:
    jobs:
      - say-hello
  validation-workflow:
    jobs:
      - validation-job
      - build-job-approval:
          type: approval
          requires:
            - validation-job
      - build-job:
          requires:
            - build-job-approval
      - deploy-job:
          requires:
            - build-job
          filters:
            branches:
              only: main
